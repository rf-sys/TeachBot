# Facebook API and related logic
$(document).bind 'initFacebook', ->
  $('#facebook_login_btn').click ->
    window.location = "https://www.facebook.com/v2.8/dialog/oauth?client_id=#{<%= ENV['FACEBOOK_APP_ID'] %>}&redirect_uri=http://localhost:3000/oauth/facebook"
