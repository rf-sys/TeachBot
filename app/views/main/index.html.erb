<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading typing" style="width: 180px;
        margin: 0 auto;">TeachBot</h1>
    <p class="lead text-muted">Something short and leading about the collection below—its contents, the creator, etc.
      Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
  </div>
</section>

<h1 class="display-4 text-center">Recent courses</h1>
<hr>
<% if @courses.present? %>
  <div class="card-deck">
    <% @courses.each do |course| %>
      <div class="col-3">
        <% cache course do %>
          <%= render partial: 'courses/course', locals: {course: course, last_page: true} %>
        <% end %>
      </div>
    <% end %>
  </div>
<% else %>
  <h2 class="text-center">No recent public courses</h2>
<% end %>